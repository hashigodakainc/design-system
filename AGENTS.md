# エージェント指示

## リポジトリの目的

このリポジトリは、Hashigodakaのデザインシステムの正本です。人間向けガイドだけでなく、
AIエージェントが利用できるデザイントークン、資産、実装例、配布用アダプターも管理します。

## 作業開始時

デザインに関する作業へ着手する前に `docs/design-brief.md` を読み、現在の目的、決定事項、
未決事項を確認してください。

参照例を使う場合は、出典URL、作者・提供元、ライセンス、公開可否を記録してください。
記事や公開ドキュメントへ、掲載権を確認していない第三者の画像を転載しないでください。

## 比較とフィードバック

- 選定前の複数案は、リポジトリへ混ぜず一時領域のローカルHTMLで比較します。
- 比較する差だけを変え、案名はA/B/Cなどの中立な名前にします。
- ユーザーのフィードバックで判断が変わったら、`docs/design-brief.md` の判断記録へ
  `日付、変更前、変更後、理由` を追記します。
- 選定後に、採用した方向だけをデザイン原則、トークン、資産、実装例へ反映します。

## 成果物の原則

- 人間向け説明と機械可読な定義が同じ判断根拠を参照できるようにします。
- ハーネス中立なMarkdown、JSON、CSS、画像資産などを正本とし、CodexやClaude Code向けの
  固有設定は薄いアダプターとして分離します。
- 特定のWebサイトやプロダクトだけに必要な判断を、会社共通の原則として一般化しません。

## 情報の配置（2026-07-29 合意）

- 機械が値として処理する情報（色・寸法・書体のトークン、資産のメタデータ）は
  `tokens/*.json` と `assets/manifest.json` だけに定義します。
- `docs/` の散文には、複数トークンにまたがる構成規則（面積配分など）、概念の区別
  （ボタンのPrimaryとブランドのPrimaryは別概念など）、未承認事項の判断だけを書きます。
  トークン単位の値・用途説明はJSON側の `description` が正本であり、散文へ再掲しません。
  散文からはトークン名で参照し、hex・px などの生値を直書きしません。
- 成熟度は二層で管理します。機械可読な正本はJSONの `status` フィールド、人間向けの
  利用可否説明は `docs/status.md` です。成熟度を動かすときは必ず両方をセットで更新します。
- `styles/tokens.css` と `styles/typography.css` は生成物であり手編集禁止です。
  `styles/components.css` は当面手書きの実装正本です。`guidelines/` サイトは表示面であり
  正本ではありません。トークン一覧などの表示は `tokens/*.json` から生成します。

## 更新ワークフロー（2026-07-29 合意）

1. 判断が変わったら `docs/design-brief.md` の判断記録へ日付・変更前・変更後・理由を追記する。
2. 値の変更は `tokens/*.json`・`assets/manifest.json` を編集する（定義はここだけ）。
3. `node scripts/build-tokens.mjs` で `styles/` を再生成する。
4. 横断的な判断が変わった場合のみ `docs/` の散文を更新する。成熟度が動いたらJSONの
   `status` と `docs/status.md` をセットで更新する。
5. `node scripts/validate-tokens.mjs` で検証する。
6. `main` へコミットしてpushする。MCPは正本を直読するため、pushが配布を兼ねる。

## ドキュメント言語

ユーザーから別の指定がない限り、説明ドキュメントは日本語で記述します。ファイル名、コード、
トークン名、識別子は英語を使います。

## Gitワークフロー

デフォルトブランチは `main`、リモート名は `origin` です。変更後は内容を確認し、無関係な変更を
含めず、実際の変更を表すメッセージでコミットしてpushします。
